node_modules
.next
out
dist
.vercel
.env
.env.local
.env*.local
.DS_Store
*.log
.turbo
coverage
.vitest-cache
tsconfig.tsbuildinfo
*.tsbuildinfo

# Deck + screenshot outputs (regenerated by `pnpm deck:build` and `pnpm shots`)
*.pptx
scripts/shots/

# Vision classify run output (regenerated by `pnpm vision:classify`)
scripts/vision-results.json
